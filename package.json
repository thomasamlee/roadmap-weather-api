{
  "name": "roadmap-weather-api",
  "version": "1.0.0",
  "main": "index.ts",
  "engines": {
    "node": ">=22.7.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file .env index.ts --experimental-transform-types",
    "dev": "node --env-file .env.dev index.ts --experimental-transform-types --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.5.1",
    "typescript": "^5.5.4"
  }
}
